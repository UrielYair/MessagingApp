get all messages by the username (both as sender or as receiver)
---
tags:
    - users
parameters:
    - in: path
      name: username
      type: string
      required: true
responses:
    200:
        description: OK
        schema:
            $ref: '#/definitions/Message'
